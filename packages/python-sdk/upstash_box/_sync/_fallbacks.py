# HAND-WRITTEN — the generator never overwrites this file.
#
# If a construct in upstash_box/_async/ can't be transformed cleanly by
# scripts/generate_sync.py, implement the sync version here and import it from the
# generated client instead of contorting the async source. Currently empty.
