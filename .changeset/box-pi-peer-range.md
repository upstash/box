---
"@upstash/box-pi": patch
---

widens the pi-coding-agent peer range to >=0.79.0 <1.0.0 (npm install failed with ERESOLVE next to Pi >= 0.80), adds a gallery image to the pi manifest, and simplifies the README
