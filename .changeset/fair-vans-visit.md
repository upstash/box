---
"@upstash/box": patch
---

Support Jev browser actions with `model: "vercel/typesafe-ai/jev"`, named input variables, scoped target discovery, action timeouts, a configurable Jev confidence threshold (default 0.8), and variable-backed replay.
