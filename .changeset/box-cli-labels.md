---
"@upstash/box-cli": patch
---

Add box labels to the CLI: `--label` (repeatable) on `box create`, `--label` filter plus a LABELS column on `box list`, and a `box labels` command group (`add` / `remove` / `list`) to manage labels on a box.
