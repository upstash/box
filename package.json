{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "vitest run",
    "test:integration": "vitest run -c packages/sdk/vitest.integration.config.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "dotenv": "^17.3.1",
    "vitest": "^4.0.18"
  }
}
